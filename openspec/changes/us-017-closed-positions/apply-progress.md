# Apply Progress — US-017

## Status: COMPLETE

All requirements applied. Pending: `npm run typecheck` and `npm run lint` verification by user.

### Applied
- [x] REQ-001 — `GET /api/portfolio/closed` endpoint
- [x] REQ-002 — `getClosedPositions` service method
- [x] REQ-003 — Zod schemas (ClosedCycle, ClosedTokenGroup, ClosedPositionsResponse)
- [x] REQ-004 — `useClosedPositions` hook
- [x] REQ-005 — `ClosedPositionsSection` component
- [x] REQ-006 — Dashboard pill-toggle (Activos | Cerrados | Todos)
- [x] REQ-007 — `PositionCyclesSection` in token detail

### Skipped (per user request)
- Tests / TDD
- Quality gates (typecheck, lint) — user runs manually
